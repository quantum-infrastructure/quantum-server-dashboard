
 query getAllGameInstances($skip: Int, $take: Int) {
    getAllGameInstances(skip: $skip, take: $take) {
      success
  totalCount
      data {
        id
        state
updated

      }
      
    }
  }

